/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./App.{js,jsx,ts,tsx}", "./<custom directory>/**/*.{js,jsx,ts,tsx}"], theme: {
    extend: {},
  },
  plugins: [],
}
//Added tailwind (nativewind) for styles but not currently in use (23.8.2024) so I can learn React Native.

